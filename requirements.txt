Flask==2.3.3
flask-mysqldb==1.0.1
face-recognition==1.3.0
opencv-python==4.8.1.78
speechrecognition==3.10.0
numpy==1.24.3
PyMySQL==1.1.0
Werkzeug==2.3.7
python-dotenv==1.0.0
scikit-learn==1.3.0
librosa==0.10.1
wave
audioop
INSERT INTO attendance (student_id, timestamp, verification_method, confidence_score)
    -> SELECT
    ->     s.id,
    ->     -- 1. Create the Timestamp: (Today - N days) + (Random time between 09:00 and 10:00 AM)
    ->     DATE_ADD(
    ->         DATE_SUB(CURDATE(), INTERVAL seq.n DAY),
    ->         INTERVAL (9 + (RAND() * 60)) MINUTE
    ->     ),
    ->     'manual_backfill', -- Mark as backfill so you know it wasn't real
    ->     ROUND(0.75 + (RAND() * 0.24), 2) -- Random confidence between 75% and 99%
    -> FROM
    ->     students s
    -> CROSS JOIN (
    ->     -- 2. Generate a list of numbers from 0 to 29 (Last 30 days)
    ->     SELECT 0 AS n UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL SELECT 4
    ->     UNION ALL SELECT 5 UNION ALL SELECT 6 UNION ALL SELECT 7 UNION ALL SELECT 8 UNION ALL SELECT 9
    ->     UNION ALL SELECT 10 UNION ALL SELECT 11 UNION ALL SELECT 12 UNION ALL SELECT 13 UNION ALL SELECT 14
    ->     UNION ALL SELECT 15 UNION ALL SELECT 16 UNION ALL SELECT 17 UNION ALL SELECT 18 UNION ALL SELECT 19
    ->     UNION ALL SELECT 20 UNION ALL SELECT 21 UNION ALL SELECT 22 UNION ALL SELECT 23 UNION ALL SELECT 24
    ->     UNION ALL SELECT 25 UNION ALL SELECT 26 UNION ALL SELECT 27 UNION ALL SELECT 28 UNION ALL SELECT 29
    -> ) seq
    -> WHERE
    ->     -- 3. Exclude Weekends (1=Sunday, 7=Saturday)
    ->     DAYOFWEEK(DATE_SUB(CURDATE(), INTERVAL seq.n DAY)) NOT IN (1, 7)
    ->
    ->     -- 4. Randomly mark absent (Remove this line if you want 100% attendance)
    ->     AND RAND() > 0.1;